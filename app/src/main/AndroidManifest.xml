<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sistema.Fraseodiccionario">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_appicon"
        android:label="Fraseodiccionario"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AclaracionActivity" />
        <activity android:name=".CreditosActivity" />
        <activity android:name=".AyudaActivity" />
        <activity
            android:name=".MainActivity"
            android:label="Fraseodiccionario"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
            android:name=".MostradorDeFrases"
            android:label="@string/title_activity_caso_letra1"
            android:theme="@style/AppTheme.NoActionBar"
            android:configChanges="orientation|screenSize" />
    </application>

</manifest>